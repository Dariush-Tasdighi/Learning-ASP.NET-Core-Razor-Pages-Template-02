@page
@model Server.Pages.Admin.PageCategories.DetailsModel

@{
	var pageTitle =
		$"{Resources.ButtonCaptions.Details} {Resources.DataDictionary.PageCategory}";

	ViewData[Constants.ViewDataKeyName.PageTitle] = pageTitle;
}

<section-form>
	<fieldset>
		<section-form-header>
			@(pageTitle)
		</section-form-header>

		<partial name="PartialViews/_DisplayPageMessages" />

		<readonly-input asp-for="ViewModel.Id" />

		<readonly-input asp-for="ViewModel.Name" />

		<readonly-checkbox asp-for="ViewModel.IsActive" />

		<readonly-input asp-for="ViewModel.Ordering" />

		<readonly-input asp-for="ViewModel.PageCount" />

		<readonly-input asp-for="ViewModel.InsertDateTime" />

		<readonly-input asp-for="ViewModel.UpdateDateTime" />

		<readonly-textarea asp-for="ViewModel.Description" />
	</fieldset>

	<section-form-footer>
		<a asp-page="Index">
			@(Html.DtatGetLinkCaptionForList())
		</a>
		<a asp-page="Update" asp-route-id="@(Model.ViewModel.Id)">
			@(Html.DtatGetLinkCaptionForUpdate())
		</a>
		<a asp-page="Delete" asp-route-id="@(Model.ViewModel.Id)">
			@(Html.DtatGetLinkCaptionForDelete())
		</a>
	</section-form-footer>
</section-form>
